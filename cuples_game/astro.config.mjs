import { defineConfig } from 'astro/config';

// GitHub Pages project site: set `site` and `base` (repo name as path).
// Example: site: 'https://sui15.github.io', base: '/cuples_game',
export default defineConfig({
  output: 'static',
});
